{% extends 'base.html.twig' %}

{% block title %}Mon compte - La Boutique Française{% endblock %}

{% block content %}
    <h1>Mon compte</h1>
    {% if is_granted('ROLE_ADMIN') %}
    <small><a href="/admin">Vous êtes un administrateur, accéder au backoffice</a></small> </br>
    {% endif %}
    Bienvenue {{ app.user.firstname }} dans votre compte.<br/>
    C'est dans cet espace que vous allez pouvoir gérer toute vos informations personnelles.
    <hr>
    <div class="row">
        <div class="col-md-4">
            <div class="account-item text-center">
                <a href="{{ path('account_password') }}">
                <img src="{{ asset('assets/img/password.png') }}" alt="Modifier mot de passe"><br/>
                Modifier mot de passe</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <a href="{{ path('account_address') }}">
                <img src="{{ asset('assets/img/location.png') }}" alt="Gérer mes adresse"><br/>
                Gérer mes adresse</a>
            </div>
        </div>
        <div class="col-md-4">
            <div class="account-item text-center">
                <a href="{{ path('account_order') }}">
                <img src="{{ asset('assets/img/shopping.png') }}" alt="Mes commandes"><br/>
                Mes commandes</a>
            </div>
        </div>
    </div>
    <hr>
{% endblock %}
